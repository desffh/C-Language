#include <iostream>

using namespace std;

int main()
{
#pragma region 연관 컨테이너
	// 컨테이너 내의 잘들이 규칙에 따라 
	// 정렬되어 있는 컨테이너입니다.
	
	//std::set<int> set;
	//
	//std::insert(50);
	//std::insert(10);
	//std::insert(50);
	//std::insert(50);
	//std::insert(50);

	// set의 경우 KEY값이 중복이 되면 값이 그대로 들어옵니다.

#pragma endregion



	return 0;
}

